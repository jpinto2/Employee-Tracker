{
  "name": "employee-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "seed": "mysql -u root -p < db/schema.sql && mysql -u root -p employee_db < db/seeds.sql"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "inquirer": "8.2.4",
    "mysql2": "^2.2.5"
  }
}