{
  "name": "employee-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "mysql -u root -p < db/schema.sql && mysql -u root -p employees_db < db/seeds.sql"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console.table": "^0.10.0",
    "inquirer": "8.2.4",
    "mysql2": "^2.2.5"
  }
}